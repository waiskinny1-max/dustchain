# Benchmarks

Benchmark numbers must be generated locally.

Run:

```bash
dust bench
dust bench --markdown > BENCHMARKS.md
```

Do not commit placeholder benchmark values as if they were measured results.
